version: '3.7'

# 컴퓨터들
services:
  # 컴퓨터이름
  backend-service:
    build:
      # 경로, 파일이름
      context: .
      dockerfile: Dockerfile
    ports:
      - '4000:4000'

      # 컴퓨터이름
  database-service:
    build:
      # 경로, 파일이름
      context: .
      dockerfile: Dockerfile_mongodb
    ports:
      - '27017:27017'
